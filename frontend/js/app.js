// Replace all API URLS for production use
const backendBaseUrl = "https://travelfrom.onrender.com";

fetch(`${backendBaseUrl}/api/travel`, {...});
fetch(`${backendBaseUrl}/api/travel/check?email=${email}`, {...});
